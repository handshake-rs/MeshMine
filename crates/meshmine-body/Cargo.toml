[package]
name = "meshmine-body"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Stable body packages and coinbase commitments for MeshMine"

[dependencies]
meshmine-codec = { path = "../meshmine-codec" }
meshmine-hns = { path = "../meshmine-hns" }
meshmine-storage = { path = "../meshmine-storage" }
meshmine-types = { path = "../meshmine-types" }
reed-solomon-erasure.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = "3.20.0"
